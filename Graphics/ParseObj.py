#Should take an OBJ file and return a list of triangle faces, ready to draw in OpenGL
#First take verticies, then textures, then normals, then faces
#Then use the faces to get correct order of verts.
#Maybe put them in a triangle strip.
